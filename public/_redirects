# Netlify redirects file for SPA routing
# This file should be placed in the public directory so it gets copied to dist during build

# Redirect all routes to index.html for client-side routing
# The 200 status means it's a rewrite, not a redirect (better for SEO)
/*    /index.html   200

